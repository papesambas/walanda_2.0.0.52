{% extends 'base.html.twig' %}

{% block title %}Statuts
{% endblock %}

{% block body %}
    <h1 class="fst-italic text-center bg-warning-subtle bg-opacity-75">Liste des élèves avec le statut :
        {{ statut }}</h1>

    {% include 'eleves/liste.html.twig' with {'eleves': eleves} %}

    <a class="btn btn-primary btn-sm" href="{{ path('app_eleves_index') }}">Retour</a>

    {#<a href="{{ path('app_statuts_edit', {'slug': statut.slug}) }}">edit</a>#}

    {{ include('statuts/_delete_form.html.twig') }}
{% endblock %}
